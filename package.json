{
  "name": "rotomi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "elm-live src/Main.elm --open -- --output=app.js",
    "build": "elm make src/Main.elm --optimize --output=dist/app.js && cp index.html dist/index.html && cp url-params.js dist/url-params.js",
    "test": "elm-test && node --test scripts/url-params.test.js",
    "test:watch": "elm-test --watch",
    "format": "elm-format src/ tests/ --yes",
    "validate-fixtures": "elm make src/ValidateFixtures.elm --output=scripts/validate-fixtures-elm.js && node scripts/validate-fixtures.js",
    "card-count-check:build": "elm make src/CardCountCheck.elm --output=scripts/card-count-check-elm.js",
    "card-count-check": "npm run card-count-check:build && node scripts/card-count-check.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "elm-format": "^0.8.8",
    "elm-live": "^4.0.2",
    "elm-test": "^0.19.1-revision17"
  }
}
